import './styles.css'; import { generateCharacter } from './domain/character'; import { createInitialState } from './domain/simulation'; import type { Character } from './domain/types'; import { createGameController } from './game-controller'; import { createSaveStore } from './persistence/save-store'; import { createCanvasRenderer } from './ui/canvas-renderer'; import { createPanels } from './ui/panels';
const canvas=document.querySelector<HTMLCanvasElement>('#game-canvas'),modal=document.querySelector<HTMLElement>('#modal-root'),hud=document.querySelector<HTMLElement>('#hud'),toasts=document.querySelector<HTMLElement>('#toast-root');if(!canvas||!modal||!hud||!toasts)throw new Error('Application shell is incomplete.');
const store=createSaveStore(localStorage),renderer=createCanvasRenderer(canvas);let controller:ReturnType<typeof createGameController>|undefined;
function start(character:Character){controller?.destroy();controller=createGameController(createInitialState(character),renderer,panels,store,hud!,toasts!)}
const panels=createPanels(modal,{onGenerate:()=>start(generateCharacter(Date.now())),onImport:start,onReset:()=>{store.clear();location.reload()}});
const loaded=store.load();if(loaded.kind==='loaded'){controller=createGameController(loaded.state,renderer,panels,store,hud,toasts)}else{panels.showStart();if(loaded.kind==='corrupt'||loaded.kind==='newer-version')setTimeout(()=>alert('Saved progress could not be loaded. Use Help to reset it after exporting diagnostics from localStorage.'),50)}
